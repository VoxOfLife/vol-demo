import { AvailabilityBlockFactory } from './availability-block-factory';

describe('AvailabilityBlockFactory', () => {
  it('should be defined', () => {
    expect(new AvailabilityBlockFactory()).toBeDefined();
  });
});
