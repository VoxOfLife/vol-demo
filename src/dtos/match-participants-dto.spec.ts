import { MatchParticipantsDto } from './match-participants-dto';

describe('MatchParticipantsDto', () => {
  it('should be defined', () => {
    expect(new MatchParticipantsDto()).toBeDefined();
  });
});
